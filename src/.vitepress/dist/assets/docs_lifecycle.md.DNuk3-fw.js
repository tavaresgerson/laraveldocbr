import{_ as e,c as o,o as a,a4 as r}from"./chunks/framework.nQaBHiNx.js";const h=JSON.parse('{"title":"Ciclo de Vida","description":"","frontmatter":{},"headers":[],"relativePath":"docs/lifecycle.md","filePath":"docs/lifecycle.md"}'),s={name:"docs/lifecycle.md"},d=r('<h1 id="ciclo-de-vida" tabindex="-1">Ciclo de Vida <a class="header-anchor" href="#ciclo-de-vida" aria-label="Permalink to &quot;Ciclo de Vida&quot;">​</a></h1><h2 id="introducao" tabindex="-1">Introdução <a class="header-anchor" href="#introducao" aria-label="Permalink to &quot;Introdução&quot;">​</a></h2><p>Ao usar uma ferramenta no &quot;mundo real&quot;, você se sente mais confiante quando entende como aquele instrumento funciona. O desenvolvimento de aplicativos não é diferente. Quando você compreende o funcionamento das ferramentas de desenvolvimento, se sente mais confortável e seguro ao utilizá-las.</p><p>O objetivo deste documento é dar-lhe uma boa visão geral do funcionamento do framework Laravel. Conhecendo melhor o conjunto global de recursos, tudo parecerá menos &quot;mágico&quot; e você estará mais confiante ao criar suas aplicações. Se não entender alguns termos imediatamente, não se desespere! Tente ter uma noção básica do que está a acontecer e sua experiência crescerá à medida que explorar outros conteúdos da documentação.</p><h2 id="visao-geral-do-ciclo-de-vida" tabindex="-1">Visão geral do ciclo de vida <a class="header-anchor" href="#visao-geral-do-ciclo-de-vida" aria-label="Permalink to &quot;Visão geral do ciclo de vida&quot;">​</a></h2><h3 id="primeiros-passos" tabindex="-1">Primeiros passos <a class="header-anchor" href="#primeiros-passos" aria-label="Permalink to &quot;Primeiros passos&quot;">​</a></h3><p>O ponto de entrada para todas as solicitações na aplicação Laravel é o arquivo <code>public/index.php</code>. Todas as solicitações são encaminhadas para este arquivo pela configuração do servidor web (Apache/Nginx). O arquivo <code>index.php</code> não contém muitos códigos, ele simplesmente serve como ponto de partida para carregar o restante do framework.</p><p>O arquivo <code>index.php</code> carrega a definição do autoloader gerado pelo Composer e, em seguida, recupera uma instância da aplicação Laravel de <code>bootstrap/app.php</code>. A primeira ação realizada pelo próprio Laravel é criar uma instância da aplicação/<a href="/docs/container.html">container de serviços</a>.</p><h3 id="kernel-http-console" tabindex="-1">Kernel HTTP/Console <a class="header-anchor" href="#kernel-http-console" aria-label="Permalink to &quot;Kernel HTTP/Console&quot;">​</a></h3><p>Em seguida, o pedido recebido é enviado ao kernel de HTTP ou ao kernel do console, utilizando os métodos <code>handleRequest</code> ou <code>handleCommand</code> da instância da aplicação, dependendo do tipo de pedido que entra na aplicação. Estes dois kernels servem como localização central através da qual todos os pedidos fluem. Por enquanto, vamos nos concentrar apenas no kernel HTTP, uma instância do <code>Illuminate\\Foundation\\Http\\Kernel</code>.</p><p>O kernel HTTP define um array de <code>bootstrappers</code> que serão executados antes da execução do pedido. Esses bootstrappers configuram o controle de erros, configuram a logon, <a href="/docs/configuration.html#environment-configuration">detectam o ambiente da aplicação</a>, e realizam outras tarefas que precisam ser feitas antes que o pedido seja realmente processado. Normalmente, essas classes controlam a configuração interna do Laravel com as quais você não tem necessariamente que se preocupar.</p><p>O kernel HTTP também é responsável por passar o pedido através da pilha de middleware do aplicativo. Esses middlewares lidam com a leitura e escrita na sessão <a href="/docs/session.html">HTTP</a>, determinando se o aplicativo está em modo de manutenção, <a href="/docs/csrf.html">verificando o token CSRF</a> e muito mais. Falaremos melhor sobre isso brevemente.</p><p>A assinatura do método <code>handle</code> para o kernel do HTTP é bastante simples. Ela recebe um <code>Request</code> e retorna uma <code>Response</code>. Pense no kernel como sendo uma grande caixa preta que representa toda a sua aplicação. Forneça-lhe solicitações HTTP e ele irá retornar respostas HTTP.</p><h3 id="prestadores-de-servicos" tabindex="-1">Prestadores de serviços <a class="header-anchor" href="#prestadores-de-servicos" aria-label="Permalink to &quot;Prestadores de serviços&quot;">​</a></h3><p>Uma das ações de inicialização do kernel mais importantes é carregar os <a href="/docs/providers.html">provedores de serviço</a> para sua aplicação. Os provedores de serviços são responsáveis por inicializar todos os vários componentes do framework, tais como o componente de banco de dados, fila, validação e roteamento.</p><p>O Laravel irá iterar esta lista de provedores e instanciar cada um deles. Após a instânciação dos provedores, o método <code>register</code> será chamado em todos eles. Em seguida, depois que todos os provedores tiverem sido registrados, o método <code>boot</code> será chamado em cada provedor. Isso é necessário para que os provedores de serviços possam depender da vinculação a todo contêiner estar registrada e disponível no momento em que seu método <code>boot</code> for executado.</p><p>Em essência, todos os recursos principais oferecidos pelo Laravel são iniciados e configurados por um Service Provider. Como eles inicializam e configuram muitos dos recursos oferecidos pelo framework, os serviços provedores são o aspecto mais importante de todo o processo de inicialização do Laravel.</p><p>Apesar de o framework usar internamente dezenas de provedores de serviços, você também tem a opção de criar os seus próprios. É possível encontrar uma lista dos provedores de serviços definidos pelo usuário ou de terceiros que sua aplicação está usando no arquivo <code>bootstrap/providers.php</code>.</p><h3 id="roteamento" tabindex="-1">Roteamento <a class="header-anchor" href="#roteamento" aria-label="Permalink to &quot;Roteamento&quot;">​</a></h3><p>Depois que o aplicativo for inicializado e todos os provedores de serviços forem registrados, a <code>Request</code> será entregue ao roteador para despacho. O roteador despachará a solicitação para uma rota ou controlador, bem como executará qualquer middleware específico da rota.</p><p>Os middlewares fornecem um mecanismo conveniente para filtrar ou examinar os pedidos HTTP que entram na sua aplicação. Por exemplo, o Laravel inclui um middleware que verifica se o usuário da sua aplicação está autenticado. Se o usuário não estiver autenticado, o middleware redirecionará o usuário para a tela de login. No entanto, se o usuário estiver autenticado, o middleware permitirá que a solicitação avance mais na aplicação. Alguns middlewares são atribuídos a todas as rotas da aplicação, como <code>PreventRequestsDuringMaintenance</code>, enquanto outros apenas são atribuídos a rotas específicas ou grupos de rotas. Você pode saber mais sobre o middleware lendo a documentação completa de <a href="/docs/middleware.html">middlewares</a>.</p><p>Se o pedido passar por todos os middlewares atribuídos da rota combinada, a rota ou o método do controlador serão executados e a resposta retornada pelo método da rota ou do controlador será enviada de volta pela cadeia de middlewares da rota.</p><h3 id="finalizando" tabindex="-1">Finalizando <a class="header-anchor" href="#finalizando" aria-label="Permalink to &quot;Finalizando&quot;">​</a></h3><p>Depois que o método do controlador ou da rota retornar uma resposta, esta será enviada para a frente, passando pelo middleware do caminho, dando à aplicação a chance de modificar ou analisar a resposta enviada.</p><p>Por fim, uma vez que a resposta retorne ao middleware, o método <code>handle</code> do núcleo HTTP devolve o objeto de resposta para o <code>handleRequest</code> da instância da aplicação, e este método chama o método <code>send</code> no objeto de resposta retornado. O método <code>send</code> envia o conteúdo da resposta ao navegador do usuário. Agora completámos a nossa jornada através do ciclo de vida de solicitação completo do Laravel!</p><h2 id="concentre-se-nos-provedores-de-servicos" tabindex="-1">Concentre-se nos provedores de serviços <a class="header-anchor" href="#concentre-se-nos-provedores-de-servicos" aria-label="Permalink to &quot;Concentre-se nos provedores de serviços&quot;">​</a></h2><p>Os provedores de serviços são verdadeiramente a chave para iniciar uma aplicação Laravel. A instância da aplicação é criada, os provedores de serviços são registrados e o pedido é entregue à aplicação inicializada. É tão simples assim!</p><p>Dominar a construção e inicialização de uma aplicação Laravel através de fornecedores de serviços é muito útil. Os fornecedores de serviço personalizados da sua aplicação são armazenados no diretório <code>app/Providers</code>.</p><p>Por padrão, o <code>AppServiceProvider</code> é bastante vazio. Este provedor é um ótimo lugar para adicionar os próprios recursos de inicialização da aplicação e vinculamentos do contêiner de serviço. Para aplicações grandes, você pode querer criar vários provedores de serviços, cada um com inicialização mais granular para serviços específicos utilizados por sua aplicação.</p>',29),i=[d];function t(n,c,l,p,m,u){return a(),o("div",null,i)}const f=e(s,[["render",t]]);export{h as __pageData,f as default};
